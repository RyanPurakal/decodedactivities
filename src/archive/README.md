# Archived: Bill Audit

The previous decodedactivities app (six-section Bill Audit). It is not shown on the live site.

To restore it, move `BillAuditApp.tsx`, `data.ts`, and `index.css` back to `src/`, point `index.html` at `/src/main.tsx`, and re-mount the React app in `main.tsx`.
